generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model champions {
  id   String  @id @db.VarChar(100)
  name String? @db.VarChar(50)
  cost Int?
}
